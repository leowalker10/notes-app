extends layout.pug

block content 
  .container 
    h1 All student datas 

    each note in notes 
      div.note-wrapper
        a.note(href=`/notes/${ note.id }`) #{ note.title } 
        a.delete(href=`/${note.id}/delete` onClick="return confirm('Are you sure?')") delete 